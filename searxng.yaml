name: xng
description: SearXNG search (opens browser, -f for CLI output)
flags:
  -f, --format=: output format (enables CLI mode)
  -e, --engine=: search engine (chainable)
  -c, --category=: search category (chainable)
  -l, --lang=: language filter
  -p, --page=: page number

completion:
  flag:
    format: ["json", "html", "csv", "rss"]
    engine: ["$(source ~/proj/searxng.sh/searxng.sh && searxng_complete_engines)"]
    category: ["general", "images", "videos", "news", "music", "files", "it", "science", "social media"]
    lang: ["en", "de", "fr", "es", "it", "pt", "nl", "ru", "zh", "ja", "ko", "ar", "he"]
