name: SearXNG
description: SearXNG Metasearch
flags:
  -q, --query=: search query
  -e, --engine=: engine to use
  -f, --format=: output format
completion:
  flag:
    format: ["json", "html"]
    engine: ["google", "brave", "piratebay", "reddit"]
